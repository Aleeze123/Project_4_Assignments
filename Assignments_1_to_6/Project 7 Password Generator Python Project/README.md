# Password Generator 🔐

This **Password Generator** Python project allows you to generate secure passwords based on customizable options like length, uppercase letters, numbers, and special characters. The generated password is displayed in a user-friendly and colorful format.


### 🛠️ Installation

To run this program, follow these steps:

1. **Clone the Repository**:
    ```bash
      git clone https://github.com/Aleeze123/Project_4_Assignments
    ```

2. **Install Dependencies**:
    The program uses the `termcolor` library for colorful terminal output. Install it by running:
    ```bash
    pip install termcolor
    ```

3. **Run the Program**:
    Once the dependencies are installed, run the program:
    ```bash
    python main.py
    ```
